import React from 'react';
import { UploadFileContaner } from './UploadFile.styles';

const UploadFile = (): JSX.Element => {
  return (
    <UploadFileContaner>
      <p>Info o Upload File</p>
    </UploadFileContaner>
  );
};

export default UploadFile;
